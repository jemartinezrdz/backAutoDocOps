# AutoDocOps Environment Configuration Template
# Copy this file to .env.dev (for local development) and fill with actual values
# ⚠️ NEVER commit .env.dev to Git - it should contain real secrets

# =============================================================================
# DATABASE CONFIGURATION (Critical - Required)
# =============================================================================
ConnectionStrings__DefaultConnection=Host=localhost;Database=autodocops;Username=postgres;Password=postgres
ConnectionStrings__Redis=localhost:6379

# =============================================================================
# JWT AUTHENTICATION (Critical - 32+ characters required)
# =============================================================================
JwtSettings__SecretKey=your_secure_jwt_secret_key_minimum_32_characters_here
JwtSettings__Issuer=AutoDocOps
JwtSettings__Audience=AutoDocOps.API

# =============================================================================
# THIRD-PARTY SERVICES
# =============================================================================
# OpenAI Configuration
OpenAI__ApiKey=your_openai_api_key_here
OpenAI__Endpoint=your_azure_openai_endpoint_if_using_azure
USE_FAKE_LLM=true

# Stripe Configuration  
Stripe__SecretKey=sk_test_your_stripe_secret_key_here
Stripe__WebhookSecret=whsec_your_stripe_webhook_secret_here

# Supabase (if using)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# S3/R2 Configuration (if using)
S3_ACCESS_KEY=your_s3_access_key_here
S3_SECRET_KEY=your_s3_secret_key_here

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000

# =============================================================================
# SECURITY NOTES & ROTATION SCHEDULE
# =============================================================================
# 1. Secrets Rotation Schedule:
#    - JwtSettings__SecretKey: Every 6 months / after leak
#    - ConnectionStrings__DefaultConnection: On password changes  
#    - OpenAI__ApiKey: Monthly
#    - Stripe__*: Upon Stripe refresh
#    - S3_*: Annually
#
# 2. Environment-specific deployment:
#    - Local: .env.dev (not committed)
#    - CI: GitHub Secrets  
#    - Production: Fly Secrets / Azure Key Vault
#
# 3. Rule: "A secret value should never appear in a versioned file, artifact, or Docker layer."

